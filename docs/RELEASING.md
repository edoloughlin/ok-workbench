# Release process

Before a release, run the clean build, tests, and `npm pack --dry-run`; inspect distributable contents, dependency and license audits, secret scanning, and the Linux isolation checks. Create a prerelease before a stable tag.
