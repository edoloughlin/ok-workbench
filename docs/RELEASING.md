# Release process

Before publishing, run the clean build, tests, and `npm pack --dry-run`; inspect package content, dependency and license audits, secret scanning, and the Linux isolation checks. Publish a prerelease before a stable tag.
